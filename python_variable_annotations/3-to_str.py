#!/usr/bin/env python3
''' Description:defines a Python function called
to_str that takes a single float argument n and
returns a string representation of n
    Arguments: n: float
'''


def to_str(n: float) -> str:
    ''' Return n value as string. '''
    return str(n)
